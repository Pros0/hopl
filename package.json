{
  "name": "hopl",
  "version": "1.0.0",
  "description": "We connect people with needs",
  "main": "index.js",
  "repository": "git@github.com:Pros0/hopl.git",
  "author": "Pros0",
  "license": "MIT",
  "private": false,
  "scripts": {
    "postinstall": "yarn install --cwd hopl-api",
    "start:api": "yarn --cwd hopl-api start",
    "start:app": "yarn --cwd app dev",
    "start": "concurrently -k \"yarn start:api\" \"yarn start:app\""
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
